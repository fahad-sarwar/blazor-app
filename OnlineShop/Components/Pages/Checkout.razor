@page "/checkout"

@using OnlineShopUI.ViewModels
@using OnlineShopUI.Services
@using OnlineShopUI.Components.Shared
@rendermode InteractiveServer
@inject NavigationManager Navigation
@inject BasketService BasketService
@inject CheckoutService CheckoutDummyDataService
@inject CustomerService CustomerService
@inject OrderService OrderService
@inject CustomAuthenticationStateService CustomAuthenticationStateProvider

@if (_errorMessage != null)
{
    <div class="alert alert-danger">@_errorMessage</div>
}

<h3 class="mb-4">Checkout</h3>

@if (CustomAuthenticationStateProvider.IsUserAuthenticated())
{
    @if (_basket == null || !_basket.Items.Any())
    {
        <div class="alert alert-warning">Your basket is empty.</div>
    }
    else
    {
        <div class="row g-5">
            <div class="col-md-7">
                <EditForm Model="@_checkoutModel" OnValidSubmit="SubmitOrder" Context="editContext">
                    <DataAnnotationsValidator />
                    <CheckoutCustomerDetails CheckoutModel="@_checkoutModel" />
                    <CheckoutBillingAddressDetails CheckoutModel="@_checkoutModel" />
                    <CheckoutShippingAddressDetails CheckoutModel="@_checkoutModel" />
                    <CheckoutPaymentDetails CheckoutModel="@_checkoutModel" />
                </EditForm>
            </div>

            <div class="col-md-5">
                <BasketSummary Basket="@_basket" />
            </div>
        </div>
    }
}
else
{
    <LoginRegisterAlert ReturnUrl="%2Fcheckout" />
}

@code {
    private BasketViewModel? _basket;
    private string? _errorMessage;

    private CheckoutViewModel _checkoutModel = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadBasketAsync();
        await CreateDummyCheckoutModel();
    }

    private async Task CreateDummyCheckoutModel()
    {
        var userInfo = CustomAuthenticationStateProvider.GetUserInfoDetails();
        _checkoutModel = await CheckoutDummyDataService.GetDummyCheckoutModel(userInfo, CustomerService);
    }

    private async Task LoadBasketAsync()
    {
        _basket = await BasketService.GetBasket();

        if (_basket != null)
        {
            _errorMessage = null;
        }
        else
        {
            _errorMessage = "Couldn't load basket.";
            _basket = new BasketViewModel();
        }

        StateHasChanged();
    }

    private async Task SubmitOrder()
    {
        var createdOrder = await OrderService.CreateOrder(_checkoutModel, _basket);

        if (createdOrder != null)
        {
            Navigation.NavigateTo($"/order-confirmation/{createdOrder.Id}");
        }
        else
        {
            _errorMessage = "Failed to place order.";
        }
    }
}